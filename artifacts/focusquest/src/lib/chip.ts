// Shared pill treatment for Now-screen chips (brain check-in, evening
// reflection) — one string so the chip row reads as a single grammar.
export const CHIP_PILL_CLASS =
  "inline-flex items-center gap-2 rounded-full border border-primary/25 bg-primary/[0.04] px-3 py-1.5 text-xs text-foreground hover:border-primary/50 transition-colors";
